================================================================
THE KUMAR METHOD — PROJECT CONTEXT & AGENT HANDOFF
================================================================
Last updated: 2026-08-10
Repo: https://github.com/Danger-Testing/kumar-method-temp (private)
Live: https://plain-rules.vercel.app (Vercel team: dangertesting,
      project name: plain-rules)
Local dev: npm run dev -- --port 3777 (owner usually has it running)

----------------------------------------------------------------
1. WHAT THIS IS
----------------------------------------------------------------
A single-purpose website that feels like reading a very old,
treasured book. The book is "The Kumar Method": ten chapters of
plain rules about money and life, written in the voice of a
Chartered Accountant who rebuilt his life across three countries
(India -> Bahrain 1990 -> America 1995; wife Sheba). The chapters
cover: the Basics, Financial Discipline, Family & Money, Friends &
Money, Investing, Betting & Predicting, Starting Your Own Company,
Moving Countries, Time Being Worth More Than Money, and Legacy.

The experience: a cinematic 3D launch intro (the physical book
spins out of darkness, its gold gilding ignites, the camera dives
into the cover) that hands off into a page-by-page reading view of
aged paper. 110 pages total: each chapter is 1 opener page (title +
italic-voiced intro with drop caps, styled after the owner's print
mockup) + 10 pages of one rule each.

The spine of the physical book carries a "ramp" logo; the ramp
logo also appears as a bronze sign-off inside the golden flash at
the end of the intro. This appears to be a Ramp (fintech) branded
artifact — treat the logo as intentional.

----------------------------------------------------------------
2. THE OWNER'S TASTE — HARD RULES, DO NOT VIOLATE
----------------------------------------------------------------
These were each learned from direct feedback. Breaking them will
get the work rejected:

- REALISM OVER FLASH. Anything that reads as a "web effect" gets
  called cheesy. The fade+drift page turn was rejected. Heavy
  deckled page edges were rejected ("in between vintage and UI").
  Fake-feeling background circles were rejected.
- Single-page turns: INSTANT on tap/click/keys; a quick leaf CURL on
  SWIPE (520ms, hinged at the spine edge — owner request 2026-08-11
  for the mobile feel). The two-page spread keeps its 740ms leaf
  flip ("really fast and really majestic"). Mobile also gets tap
  arrows flanking the scrubber, and text selection is suppressed on
  coarse pointers only (desktop copy still works — deliberate).
- ALL GLOW/LIGHT EFFECTS ARE OFF (owner call, 2026-08-11 afternoon,
  after the 3D artist reviewed): TheBook renders the artist's
  bookNew2.glb untouched — no emissive, BLOOM.intensity 0, room rig +
  emergence shade only. The code-built recreation + its dialed glow
  (IGNITE_FULL 1.65, user-chosen ×0.50) live in git history and
  experiments/glow-test-page.tsx if ever wanted back. Ask before
  reviving any of it.
- NO Contents menu / ribbon. Navigation is only: click zones
  (left third back on single page, halves on spread), arrow keys,
  space, swipe.
- NO page tilt/rotation between pages.
- NO glow behind the book in the READING view. (The intro's
  build-and-detonate glow is separate and was requested.)
- Page edges: nearly straight, long gentle waves, low-frequency.
  High-frequency edge fuzz reads as "stubble" on mobile — rejected.
- Page corners are SQUARE — decided 2026-08-10, the A/B toggle was
  removed. Do not reintroduce rounded/worn corners.
- EVERY risky/aesthetic change goes in ITS OWN COMMIT with a clear
  message, so the owner can say "revert that" and it's one
  git revert + redeploy. This is a standing contract. Honor it.
- LIGHTS ARE ON, PERIOD (owner call, 2026-08-11, after dialing the
  final glow). The lights toggle and glow slider are REMOVED. The
  plain/inspection plumbing stays dormant in TheBook/Intro3D/
  ClosedBook props for future diagnostics — no UI for it.
- The owner dictates by voice; messages arrive mid-work and may
  contain transcription noise ("she" for "you", etc.). Read intent.
- User's other conventions (from their global memory): ⌥Space is
  taken by Superwhisper; they browse in Comet + Safari; never
  launch Dia; ask before headless browser automation.

----------------------------------------------------------------
3. ARCHITECTURE
----------------------------------------------------------------
Next.js 16 App Router, TypeScript, NO Tailwind — all styling in
app/globals.css. React 19. reactStrictMode is OFF in next.config.ts
(strict-mode double-mounting created/destroyed WebGL contexts back
to back and crashed the postprocessing EffectComposer with
"Cannot read properties of null (reading 'alpha')").

Files that matter:

  lib/content.ts      — ALL book text, verbatim from the owner's
                        Notion doc (3 typos fixed with permission,
                        see §6). BOOK_TITLE = "The Kumar Method".
                        Each chapter: roman numeral, eyebrow text,
                        big-title lines, shortName (running heads),
                        intro paragraphs, exactly 10 rules.
  lib/pages.ts        — pagination (chapter opener + 1 rule/page =
                        110 pages) and seeded() PRNG. ALL visual
                        randomness (stains, smudges, paper-scan
                        sampling) is seeded by page index so SSR
                        and client render identically. NEVER use
                        Math.random() in render paths.
  components/Experience.tsx — phase machine: "intro" -> "book",
                        plus the golden handoffFlash overlay that
                        the first page emerges from (only when the
                        intro finished naturally, not skipped).
                        Skips intro on prefers-reduced-motion.
  components/Intro3D.tsx — the whole 3D launch sequence (see §4).
  components/Book.tsx — the reader (see §5).
  app/globals.css     — every visual decision lives here, heavily
                        commented by section.
  public/covers/      — front.jpeg, back.jpeg, spine.jpeg (the
                        owner's cover art), ramp.webp (logo).
  public/paper/vintage.jpg — real aged-paper scan, James Puckett,
                        CC BY 2.0, via Wikimedia Commons.
                        ATTRIBUTION LIVES IN README.md — keep it.
  public/book.glb     — DOES NOT EXIST, but if the owner ever adds
                        a generated 3D book mesh here, Intro3D
                        auto-detects (HEAD request) and uses it
                        instead of the built book.

----------------------------------------------------------------
4. THE 3D INTRO — HOW THE BOOK IS BUILT (Intro3D.tsx)
----------------------------------------------------------------
The book is NOT an AI-generated mesh and NOT a video. It is a real
3D object constructed at runtime in three.js (react-three-fiber):

- GEOMETRY: four boxes — front cover, back cover, a spine hub that
  sits slightly proud like real binding, and an inset page block.
  Proportions are MEASURED FROM THE SCANS: H=1.65, W=H*(700/1088)
  (front scan aspect), D=H*(230/852)*0.9 (spine scan aspect).
- TEXTURES: the owner's actual cover scans are mapped onto the
  outer faces. The same images double as bumpMap (embossing
  relief) and roughnessMap. Page block edges use a procedural
  gilded-stripe canvas texture.
- WINE TINT: covers are recolored per-pixel at load (useWineTint):
  non-gold pixels shift toward deep burgundy (r*0.66, g*0.52,
  b*1.45+12); gold pixels are detected and left warm. Original
  scans on disk are untouched — the tint is runtime-only.
- GOLD MASK (useGoldMask): the front + spine scans are scanned
  pixel-by-pixel (gold = r>128 && g>84 && g>r*0.5 && b<g*0.92 &&
  r+g>236, then 1.2px feather blur) to build an emissiveMap, so
  ONLY the gilded emblems/lettering ignite. Threshold is strict on
  purpose: looser values make leather pixels "sparkle" (rejected).
- TIMELINE (5.3s, driven in useFrame; clock zeroes on the FIRST
  RENDERED FRAME so slow texture loads don't eat the opening):
    0.0–3.3s  spin: rotY from 2.7π to 0, easeOutQuart; float bob;
              scale 0.84 -> 1.
    2.5–3.5s  ignition: emissive + bloom ramp (at 10% level), with
              a subtle sin() flicker like candlelight.
    3.85–5.05s dive: easeInExpo dolly from z=4.15 to z=0.43, FOV
              widens 40->55, slight camera roll. The dolly STOPS
              before the scan runs out of pixels; the last stretch
              is carried by a DOM gaussian rack-focus blur (16px
              desktop / 10px mobile) + animated film grain overlay.
    ~4.8–5.2s dissolve into GOLDEN light (not black), ramp logo in
              bronze; Experience then shows the reader under a
              1.1s handoffFlash fade-out. Skip = click or key
              (Enter/Esc/Space) — skipping cuts straight to the
              reader with NO flash.
- ATMOSPHERE: .introGlow, a blurred amber DOM layer behind the
  canvas, breathes with the ignition then DETONATES mid-dive
  (opacity/scale driven per-frame via onAtmos; burst = d*(1-d)*4).
- POST: Bloom (mipmapBlur, radius 0.42, threshold 0.55) — radius
  kept tight because a wide halo read as a fake "growing circle"
  (rejected). EffectComposer multisampling: 4 desktop, 0 on coarse
  pointers.
- MOBILE: dpr capped [1,1.5], gl antialias off (composer covers
  it), cheaper blur. Textures preload at module scope.

----------------------------------------------------------------
5. THE READER (Book.tsx + globals.css)
----------------------------------------------------------------
- LAYOUT: page proportions fixed (aspect 0.66), sized by container
  queries — ALL page typography is in cqw units relative to each
  leaf's own container (.leafBox has container-type: size). Fonts:
  EB Garamond (body, old-style numerals via "onum"), IM Fell
  English (drop caps, rule numbers, eyebrows), IM Fell English SC
  (running heads). Loaded via next/font.
- PAPER STACK (bottom to top inside .paper): tone gradients ->
  photoPaper (the real scan, multiply-blended, saturate(0.5)
  brightness(1.22) contrast(0.9), opacity 0.45, and PER-LEAF
  sampling: seeded mirror/pan/zoom so no two pages share the same
  creases) -> mirrored ghost show-through of the NEXT page ->
  seeded foxing stains + edge smudge -> content -> SVG grain
  (0.15) -> large blotch (0.08) -> edgeShade (binding-side
  darkening + worn corners). Page silhouette is cut by an SVG
  turbulence-displaced mask (low frequency 0.006/0.0045, 2
  octaves, scale 4–5; rx=12 worn variant, rx=0 square variant).
- SPREAD MODE: media query (min-width 900px + landscape) shows two
  facing pages; navigation moves 2; verso/recto edge shading is
  mirrored; folios sit on outer corners; a soft gradient spine
  runs down the center; page turns use the 3D leaf flip (§2).
  Portrait/phone = single page, instant turns.
- The chapter opener auto-shrinks type for long intros (dense /
  denser classes) and auto-fits the big title to its longest line.

----------------------------------------------------------------
6. CONTENT NOTES
----------------------------------------------------------------
- Text is otherwise VERBATIM from the owner. Do not edit voice.
- 3 typo fixes were approved: "The market is built to price...",
  "some app will cut your limits...", "work that pays well."
- OPEN ITEM — the owner must supply the year in Chapter II:
  "I qualified as a Chartered Accountant in ——". It is typeset as
  an em-dash blank. REMIND THEM when relevant.
- OPEN ITEMS: author credit line; whether a title page/front
  matter should exist (would also bring the count toward the 122
  pages the owner once estimated); repo is named -temp and may be
  renamed.

----------------------------------------------------------------
7. OPERATIONS
----------------------------------------------------------------
- Deploy: the repo IS git-connected to Vercel (discovered 2026-08-11
  after a day of redundant CLI double-deploys): push to main =
  production deploy, push to any branch = preview build. SHIP = merge
  + push, then ONE verification after Vercel's auto-build lands
  (vercel ls or a curl). Don't run `vercel --prod` on top of a push.
  (CLI is logged in as ctmventuresinc, team dangertesting; alias
  https://plain-rules.vercel.app.)
- Verify after deploy: curl the alias AND a static asset
  (/book.glb, /paper/vintage.jpg). CDN may take a few seconds; a 404
  right after aliasing is usually propagation.
- CAUTION (shared checkout): `vercel` CLI deploys upload the WORKING
  DIRECTORY, untracked files included — with git-connected deploys
  this mostly doesn't matter anymore, but never CLI-deploy while a
  peer has uncommitted/untracked files in the tree.
- REVERT PROTOCOL: find the commit in git log (messages are
  written to be revert-targets), `git revert <sha>`, rebuild,
  push, `vercel --prod --yes`. The owner explicitly wants this to
  be one-step easy. Recent revert targets of note:
    * background glow build/detonate  — "Background glow: builds..."
    * wine leather recolor            — "Dark wine leather: ..."
    * spread leaf flip + paper variety— "Spread page turns are..."
    * two-page spread itself          — "Two-page spread on wide..."
    * real paper scan + smooth edges  — "Ancient paper: ..."
    * sparkle level                   — "Sparkle at 10%: ..."
- Local dev logs land in /tmp/plain-rules-dev.log (browser console
  errors are forwarded there by Next dev with [browser] prefix).

----------------------------------------------------------------
8. KNOWN TRAPS
----------------------------------------------------------------
- Turning reactStrictMode back on will crash the intro's
  EffectComposer in dev (WebGL context churn). Leave it off or
  re-test thoroughly.
- Don't put `emissiveMap: undefined` in material constructor
  params — three.js warns; assign conditionally after creation.
- All page randomness must stay seeded (SSR/client hydration).
- The gold-mask threshold and the sparkle multipliers are TASTE
  CONSTANTS, not free parameters.
- `vercel logs <url>` follows/streams — it will hang a foreground
  shell; run with a timeout or in background.
- The repo path contains a space ("app danger") — always quote.
- NEVER apply a filter-heavy SVG data-URI (feTurbulence/
  feDisplacementMap) directly as a CSS mask-image on the page:
  Chromium intermittently fails to rasterize it and the ENTIRE page
  paints invisible. The edge masks are rasterized to PNG at runtime
  in Book.tsx (rasterizeMask -> CSS vars --paper-mask-worn/-square)
  with fallback none, so a mask failure can only mean straight
  edges, never a missing page.
- The "stars" in the intro are ON the book (soft points drawn into
  the emissive gold mask from the leather's natural glints), NOT
  floating room particles — the owner explicitly rejected drei
  Sparkles fields twice. Don't re-add them.
  UPDATE 2026-08-11: on the GLB path, stars AND the all-over gilding
  glow are removed entirely (owner step-by-step call) — the emissive
  is now the authored stencil public/masks/ramp-glow.png (back-cover
  ramp mark ONLY, built offline per the recalibration protocol).
  Don't "helpfully" restore the runtime gold mask on the GLB; ask.

GOLD MASK RECALIBRATION PROTOCOL (mandatory on every book swap):
The ignition glow is driven by buildGoldMask() in Intro3D.tsx, whose
color thresholds are TUNED TO THE CURRENT TEXTURES. A new book.glb
means new tones and the mask WILL silently miss (red-leather book =
yellow-gold rule; plum book = copper/warmth rule keyed on r-b). Any
time public/book.glb changes:
 1. Extract textures: npx @gltf-transform/cli copy book.glb out.gltf
 2. Replicate the mask pipeline in Python (PIL) against the real
    baseColor atlas and RENDER A PREVIEW IMAGE — look at it. Target:
    title/emblem/border/logo coverage ~8-10% of atlas, leather field
    completely clean, stars scattered evenly.
 3. Only then port the tuned constants into buildGoldMask() (both the
    gilding rule and the star-glint floors) and ship.
Never assume the old thresholds transfer. Never ship without seeing
the mask preview.
================================================================

================================================================
SESSION HANDOFF — STATE AS OF 2026-08-11 (late afternoon)
(The owner's thread hit context limits; a fresh session starts here.
Read this section as the CURRENT truth where it conflicts with
older sections above.)
================================================================

THE BOOK: Henry's (the 3D artist's) GLB — bookNew2, pixels UNTOUCHED
(owner+artist call after the code recreation was rejected as "not my
model"). components/TheBook.tsx is the single source of truth; the
recreation-era code paths are dead code. ALL surface effects are OFF
(no emissive/ignition/bloom on the book). Artist may deliver a
re-export fixing texture density; the recalibration protocol applies
if any glow returns.

LIGHTING: BookLights in TheBook.tsx — dim candle-GOLD rig: ambient
0.52 #f6e3bd + ONE on-axis directional #ffd98f. The directional runs
SCANNING PASSES in the intro (sweep prop): starts below the book at
zero intensity, pans up past it, fades out at the top, restarts —
sine intensity envelope makes the loop seamless. On-axis final angle
is sacred (raking angles = moiré/sparkle, banned). ClosedBook holds
steady full aim.

INTRO FLOW (Experience.tsx): auto-launches on page load (no click
gate). Emergence: dot-scale (~2-6%) from the doorway center, straight
z-zoom (easeInExpo creep→rocket), scale on its own quadratic curve
(visible from frame one), flat-to-upright single pitch, cave-shadow
darkening (shadeRef), arrival BOOM to ~1.65x then clean exponential
settle (0.18s up / ~0.4s down). Then HOLDS indefinitely (tt frozen,
bob alive) over the simmering chamber with hero copy shown — tap to
release the dive into the reader. No skip hint (retired; clicking
pre-hold still skips). Doorway blazes bright GOLDEN (255,196,64
family) with bouncing left/right wall rakes.

LEAD CAPTURE: the corner CTA is REMOVED EVERYWHERE — the clickable
tagline ("Use Ramp to run your business", 2px underline) owns modal
entry on hold view, closed view, AND a reader-corner variant. Modal:
Switzer lowercase, placeholders, dark autofill, success state hides
fields (thanks + dismiss). Supabase live BUT free tier auto-pauses:
first submission after idle takes ~15-20s (verified: warm requests
are 0.4-0.6s). PENDING OWNER DECISION: keep-alive cron vs plan
upgrade. Keyboard guards: form typing can't flip pages or skip.

MOBILE: swipe = leaf curl, tap = instant (taste rule). Text selection
off on touch only. iOS Low-Power play-glyph fix: native controls
hidden, play() retried on first touch, smoke hides if refused.

ICONS: app/icon.png + apple-icon + favicon.ico = brightened square
cut from the cover art (no letterbox). og.png is Marc's.

HERO COPY: Switzer experiment ("might revert" per owner) top+bottom
on hold/closed views, near-full alpha + back-shadow for legibility.

OPS (CURRENT): repo git-connected to Vercel — PUSH TO main = PROD
DEPLOY automatically; manual `vercel --prod` is redundant. Ship =
merge/push + one verification. Preview protection is OFF (public
links). Repo is PUBLIC. Ports: 3777 dev (this project), 3778 Marc's
reference clone. Two Claude sessions share the checkout with
claim-ping conventions (files + tree ops); races have occurred and
resolved benignly — always check git status/log before assuming
tree state.

ETERNALLY PENDING: the Chartered Accountant year in Chapter II
(lib/content.ts, "in ——"). Credits/attribution screen was being
discussed at handoff time. Supabase keep-alive decision.

----------------------------------------------------------------
ADDENDUM 2026-08-12: THE RIBBON (BookBanner)
----------------------------------------------------------------
Kendall asked for the tagline "fancier / in line with the site" and
email capture on every mobile page; the owner supplied a mock of a
parchment banner hanging from the book's bottom edge. Shipped as
components/BookBanner.tsx (commit a657f99, revert alone to restore
the old clickable tagline):

- A swallowtail (clip-path) parchment ribbon TUCKED under the book's
  bottom edge on the HOLD and CLOSED screens: tagline (Garamond, ink
  #3d2c17, ramp swoosh in ink like the reader pages), ornamental
  rule, inline "Enter your work email" + dark-red arrow submit.
  Paper = .paper gradients + vintage.jpg multiply (same as reader).
- Positioning: fixed, top min(calc(50% + 22svh), calc(100svh -
  260px)) closed / 27.5svh hold — the min() floor stops the tails
  clipping below the fold on phones (they DID clip before it).
- The clickable tagline + modal are GONE from hold/closed; the
  reader's corner tagline still opens EnterpriseAccessModal. The
  leads route now requires ONLY email (banner rows get "—" for
  name/company — tell Marc before he wonders about the dashes).
- All banner pointer events stopPropagation: clicks must not release
  the intro hold or read as tap-to-open on the closed book. The
  texture ::before would eat input clicks — the form is z:1 relative
  above it. Verified via headless Chromium (playwright-core +
  ~/Library/Caches/ms-playwright chromium): hold/closed, phone/
  desktop, focus, typing, submit success ("Thanks — we'll be in
  touch shortly.").
- Test rows to delete in Supabase leads: two "latency test - delete
  me" + banner-smoke-test-delete-me@example.com +
  banner-ui-test-delete-me@example.com.

ADDENDUM 2026-08-12 (later): ribbon interaction hardening
- ROOT CAUSE of "tapping the email field opens the book": the
  .introCanvas wrapper (z2) was hit-testing over the ribbon (z1) and
  bubbling taps to the intro root's release-click. Fixed with
  pointer-events: none on .introCanvas. bannerTapGuard
  (BookBanner.tsx) stays as the engine-agnostic backstop, called by
  both scene containers before any open/release/drag.
- Only the BOOK is clickable now (owner): .bookHotspot divs track the
  book's projected rect every frame (bookRect in TheBook.tsx) — they
  carry cursor:pointer and the containers rect-check taps against
  them. Room = arrow cursor (grab in the closed room), paper =
  default, field = text caret, seal = pointer.
- The held stack settles ~6% smaller and 0.09 world higher after the
  boom (IntroScene "settle") so the swallowtail clears the fold while
  the book bobs.

ADDENDUM 2026-08-12 (evening): the dismissed state IS the held opening
Sponsor feedback: they loved the opening hold (tail, light sweep, no
dragging) and disliked the drag-to-spin dismissed room. Now dismissing
the reader reveals the SAME held page: Intro3D gained startHeld (the
timeline begins at the hold — no emergence replay) and active=false
standby (pre-mounted invisibly under the reader; pointer-events AND
the keydown skip are dead — Esc while reading must not fire it).
Experience keys the held instance by heldGen and remounts it after
each dive (a finished timeline can't be re-held). Tap the book → the
same dive + golden flash back into the reader. The spinnable
ClosedBook is RETIRED from the flow but the component file stays
untouched as the safe copy — one import away if wanted back.

ADDENDUM 2026-08-13: the tail enters the READER
- The same parchment ribbon now hangs under the page in the reader
  (.readerTail, first child of .bookArea → painted under the leaves;
  spread: under the RIGHT page at left 75%; scaled 0.72 / 0.6 phone).
  Pages sized down + lifted for it (.bookArea 76svh/128vw, margin 13vh).
- The ramp redirect opens in a NEW TAB now (Kendall: keep reading),
  popup-blocked → same-tab fallback.
- The reader corner tagline AND the get-access modal are RETIRED —
  the tail is THE capture everywhere. EnterpriseAccessModal.tsx and
  the /api/enterprise-leads route stay in the repo, unused.
- Kendall's illustration idea (etchings on some pages) is discussed
  but NOT built.

ADDENDUM 2026-08-13 (later): gate, share, etchings
- CHAPTER GATE: lib/gate.ts (env override / one-chapter-per-day from
  UNLOCK_START / UNLOCK_ALL). Teaser leaf ends the book ("Come back
  tomorrow. Good things compound."). Set UNLOCK_START to the real
  launch date at handoff.
- SHARE MARK: ❧ + Fell italic on the folio row, corner opposite the
  folio. Touch = native sheet; desktop = clipboard + "copied".
- ETCHINGS: 3 sepia vignettes (ch I rules 4/6/10) generated with
  Gemini image gen (gemini-2.5-flash-image, key borrowed from
  crit-website/.env.local), white-pointed + feathered + baked to
  ALPHA ink PNGs offline (PIL, /tmp/illos/gen.py recipe) — CSS
  mix-blend-mode DOES NOT WORK at that DOM depth (isolated stacking
  context), alpha bake is the way. ILLUSTRATIONS map in Book.tsx.
- window.open("noopener") returns null on success — never use its
  return as a popup-blocked signal (it navigated both tabs).
